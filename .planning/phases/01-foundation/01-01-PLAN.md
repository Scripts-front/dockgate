---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "Project installs dependencies without errors using `bun install`"
    - "TypeScript type-checker passes with no errors on an empty src/index.ts"
    - "bun.lock file exists and is committed (reproducible builds)"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with correct name, dependencies, and scripts"
      contains: "express, minio, bun-types, @types/express, typescript"
    - path: "tsconfig.json"
      provides: "TypeScript config matching Bun's official recommended settings"
      contains: "Preserve, bundler, verbatimModuleSyntax"
    - path: ".gitignore"
      provides: "VCS exclusion of node_modules, .env, dist"
  key_links:
    - from: "tsconfig.json"
      to: "src/**/*.ts"
      via: "TypeScript language server"
      pattern: "\"strict\": true"
---

<objective>
Initialize the DockGate project: create package.json with all required dependencies, tsconfig.json with official Bun settings, and .gitignore. Run `bun install` to generate bun.lock.

Purpose: Provides the project foundation that all subsequent plans build on. Without this, no TypeScript type-checking or dependency resolution is possible.
Output: package.json, tsconfig.json, .gitignore, bun.lock
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json and tsconfig.json</name>
  <files>package.json, tsconfig.json</files>
  <read_first>
    - CLAUDE.md (tech stack constraints, forbidden packages, tsconfig directive)
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack section, tsconfig example)
  </read_first>
  <action>
Create `package.json` with the following exact content:

```json
{
  "name": "dockgate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^5.2.1",
    "minio": "^8.0.7"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
```

Create `tsconfig.json` with the following exact content (official Bun recommended config from bun.sh/docs/typescript):

```json
{
  "compilerOptions": {
    "module": "Preserve",
    "moduleResolution": "bundler",
    "verbatimModuleSyntax": true,
    "types": ["bun-types"],
    "noEmit": true,
    "strict": true
  },
  "include": ["src/**/*.ts"]
}
```

Note: `"types": ["bun-types"]` (not `["bun"]`) because the package is `bun-types`, not `bun`.
  </action>
  <verify>
    <automated>grep -q '"express"' /dockgate/package.json && grep -q '"minio"' /dockgate/package.json && grep -q '"bun-types"' /dockgate/package.json && grep -q '"verbatimModuleSyntax"' /dockgate/tsconfig.json && grep -q '"Preserve"' /dockgate/tsconfig.json && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"express": "^5.2.1"` under dependencies
    - package.json contains `"minio": "^8.0.7"` under dependencies
    - package.json contains `"bun-types"` under devDependencies
    - package.json contains `"@types/express"` under devDependencies
    - package.json contains `"typescript"` under devDependencies
    - tsconfig.json contains `"module": "Preserve"`
    - tsconfig.json contains `"moduleResolution": "bundler"`
    - tsconfig.json contains `"verbatimModuleSyntax": true`
    - tsconfig.json contains `"strict": true`
    - tsconfig.json contains `"noEmit": true`
  </acceptance_criteria>
  <done>Both files exist with correct content as specified above</done>
</task>

<task type="auto">
  <name>Task 2: Create .gitignore and run bun install</name>
  <files>.gitignore, bun.lock</files>
  <read_first>
    - CLAUDE.md (note: `.env` must not be committed; `.env.example` is committed)
  </read_first>
  <action>
Create `.gitignore` with the following content:

```
node_modules/
.env
dist/
*.log
.DS_Store
```

Then run `bun install` in the project root (`/dockgate`) to generate `bun.lock` and `node_modules/`. The lockfile must be committed to git (reproducible builds in CI/CD). Do NOT add `bun.lock` to .gitignore.

Verify installation succeeds with no errors. If any package resolution fails, re-check the version constraints in package.json against the npm registry.
  </action>
  <verify>
    <automated>cd /dockgate && bun install && ls bun.lock && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains `node_modules/`
    - .gitignore contains `.env`
    - .gitignore does NOT contain `bun.lock`
    - bun.lock file exists at `/dockgate/bun.lock`
    - node_modules/ directory exists containing `express` and `minio` subdirectories
    - `bun install` exits with code 0 (no errors)
  </acceptance_criteria>
  <done>bun.lock exists, node_modules installed, .gitignore excludes .env and node_modules</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→CI | package.json and bun.lock are source of truth for build reproducibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | bun.lock | mitigate | Commit bun.lock to git; CI uses `bun install --frozen-lockfile` to prevent supply chain drift |
| T-01-02 | Information Disclosure | .env | mitigate | .gitignore excludes `.env`; `.env.example` with placeholder values committed instead |
| T-01-03 | Tampering | package.json deps | accept | All packages from verified npm registry; versions pinned to minor range; no unreviewed packages |
</threat_model>

<verification>
After plan execution:
1. `cd /dockgate && bun install` — exits 0, no warnings
2. `grep "minio" /dockgate/package.json` — shows minio dependency
3. `grep "Preserve" /dockgate/tsconfig.json` — shows correct module setting
4. `ls /dockgate/bun.lock` — file exists
5. `cat /dockgate/.gitignore | grep ".env"` — .env excluded
</verification>

<success_criteria>
- package.json, tsconfig.json, .gitignore exist with correct content
- `bun install` completes without errors
- bun.lock exists and is not gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with artifacts created, decisions implemented (INFRA-04), and any notes for downstream plans.
</output>
